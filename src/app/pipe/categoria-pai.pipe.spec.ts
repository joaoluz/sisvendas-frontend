import { CategoriaPaiPipe } from './categoria-pai.pipe';

describe('CategoriaPaiPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriaPaiPipe();
    expect(pipe).toBeTruthy();
  });
});
